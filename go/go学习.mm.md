# GO配置
## GO网站
- <https://studygolang.com/dl>

## GO目录
- bin<br>第三方的可执行文件
- pkg<br>第三方的go语言包

## GO环境
- GOROOT
- GOPATH
- PATH
- GOPROXY:https://goproxy.cn,direct

## GO命令
- go version
- go env

## GO工具链
### GO开发工具
- go build:编译
- go clean:删除生成的可执行文件
- go vet:捕获错误
  - Printf类函数调用时，类型匹配错误的参数
  - 定义常用的方法时,方法签名的错误
  - 错误的结构标签
  - 没有指定字段名的结构字面量
- **go fmt:代码格式化工具**
- go doc:打开文档
### 依赖管理
- godep
- vender
- gopkg.in
- gb

# 数据类型
## 变量
- 变量声明(var)
- 变量类型
  - 整型(%d)-8个字节
  - 浮点型(%f)
    - 默认6位小数
  - 布尔型(%t)
  - 字节型(%d)
  - %p打印地址
- 变量的赋值
  - 未显式赋值的变量，其值为“0”
  - :=符号赋值，自动推断
  - 声明赋值一行完成
- %T输出变量类型

## 字符串
### 本质是不可修改的byte数组
- %c查看字母本身
- 一个汉字3个byte(utf-8)
- rune切片
- 双引号变反引号，原封不动
### 字符串拼接
- +

## array数组
### 声明和初始化
- 声明var arr [5]int
  - 数据类型明确
  - 长度明确
  - 都初始化为零值
### 使用数组
- \[\]索引
- 赋值
### 多维数组
- 使用索引为多维数组赋值
### 函数间传递数组
- 开销很大

## 切片
### 切片本质是结构体
- 三元素
  - array unsafe.Pointter指针
  - len int
  - cap int
- make创建切片
- nil切片
- append追加切片
- len查看长度
- cap查看capcity
- 数组越界
- 扩容2倍(1000元素以下，增长因子)
  - 扩容后不再共享底层空间
- 创建切片时的第三个索引
### 使用切片
- 赋值和切片:共享同一段底层数组
- 追加切片...
- 迭代切片:range
### 多维切片
### 在函数间传递切片

## map
### 类似于字典
- 无序
- var m map[string]int
### 创建和初始化
- make创建切片初始化
- 函数，切片不能为key
### 使用映射
- nil映射
- exists(返回两个):key不存在为0
- delete:删除映射
### 在函数间传递映射

# GO语言的类型系统
## 结构体
### 声明和初始化
- type struct(%v,%+v,%#v)
### 结构体方法
- 值接受者:使用值的副本来调用方法
- 指针接受者:使用实际值来调用方法

## 内置类型
- 数值类型
- 字符串类型
- 布尔类型

## 引用类型
- 切片
- 映射
- 通道
- 接口
- 函数

# 控制语句
## if语句
- else
- else if
- ;之前初始化(局部变量)

## for循环
- i++
- break
- _作为占位符

# 函数
## 函数创建
- func关键词
- 传递参数
- 返回参数类型
- return
## 函数名
## 异常就地处理
- error类型（errors包）
- nil
- if err=nil

# defer
- 注册